torch>=2.2
torchvision>=0.17
segmentation_models_pytorch>=0.4.0,<0.6   # MobileViT-XXS requires smp>=0.4 (tu- prefix); 0.3.x has no MobileViT.
timm>=0.9.7,<2.0                            # smp 0.4.x needs timm 1.0+; pin upper to avoid future breaks.
albumentations==1.4.18
opencv-python-headless
scikit-learn
scipy
numpy
tqdm
torchprofile
onnx
onnxruntime
onnxscript      # PyTorch 2.8+ dynamo-based torch.onnx.export needs this
pytest>=7.0
# optional (deployment)
# coremltools
# tensorflow
# onnx-tf
